export const metadata = {
    title: "لوحة التحكم | DropGenius AI",
    description: "إدارة متجرك، صيد المنتجات، ومتابعة الأرباح في مكان واحد.",
};

export default function DashboardLayout({ children }) {
    return <>{children}</>;
}
